#pragma once
#include <iostream>
#include <string>
#include <ctime>
#include <windows.h>
#include <fstream>

using namespace std;
int count(string);
DWORD LenAsm(char * s);
void COPYSTR(char *a, char *b);
