long fact(long x)
{
     if (x == 1)
          return 1;
     else
          return x * fact(x - 1);
}
long fact1(long x)
{
     if (x == 1)
          return 1;
     else
          return x * fact(x - 1);
}
// конец файла fact. cpp  
   
