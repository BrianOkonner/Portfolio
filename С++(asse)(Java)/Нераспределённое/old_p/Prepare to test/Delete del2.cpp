#include<iostream>
#include<iomanip>
#include "fun_h.h"
#include <time.h>
using namespace std;
void main()
{
	int i=1;
	if (i)
	else
	{if (i)
	else
	{if (i)
	else
	{if (i)
	else
	{if (i)
	else i++;}}}}
	cout<<i;
	system("pause");

}